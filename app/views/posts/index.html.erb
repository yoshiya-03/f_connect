<div class="wrapper">
  <div class="card-columns">
    <div class="row g-6" data-masonry='{"percentPosition": true }'>
      <div class="col-8 offset-md-2">
       <% @posts.each do |post| %>
          <%= link_to post_path(post.id) do %>
           <div class="card">
              <%= attachment_image_tag post, :image, :fill, 300, 300, class:"card-img-top" %>
              <div class="card-body">
               <%= attachment_image_tag post.user, :profile_image_id, size:'60x60', fallback: "no-image.jpg", class:"mt-3 rounded-circle"; %>
                <p class="card-title">title : <%= post.title %></p>
                <p>name : <%= post.user.name %></p>
                <p><%= link_to "#{post.post_comments.count} コメント", post_path(post.id) %></p>
              </div>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
